const num = 10

const Sum = (a, b) => {
    return a + b
}
export {num, Sum}

//Default export:-no need of destructuring and can be called using any name

export default print=()=>{
    return "Default function"
}
